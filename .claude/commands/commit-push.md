Commit and push the current changes:

1. Run `git status` and `git diff --stat` to see what changed
2. Stage all relevant files (skip .env, credentials, or anything sensitive)
3. Write a clear commit message that describes WHAT changed and WHY
4. Commit, then push to origin
5. Show me the commit hash when done
